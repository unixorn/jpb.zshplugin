#!/bin/bash

PS_ARGS='wax'

if [ -f /etc/debian_version ];then
  case `cat /etc/debian_version` in
    2.2)
      PS_ARGS='ax'
      ;;
    *)
      PS_ARGS='axe'
      ;;
  esac
fi

ps ${PS_ARGS} | grep $* | grep -ve grep -ve ${0}
